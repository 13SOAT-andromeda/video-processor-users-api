apiVersion: v1
kind: Service
metadata:
  name: video-processor-users-api-svc
  namespace: default
  labels:
    app: video-processor-users-api
spec:
  type: ClusterIP
  selector:
    app: video-processor-users-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
