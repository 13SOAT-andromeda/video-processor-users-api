version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - gosec
    - govet
    - errcheck
    - staticcheck
    - unused
  settings:
    gosec: {}

formatters:
  enable:
    - gofmt
    - goimports
